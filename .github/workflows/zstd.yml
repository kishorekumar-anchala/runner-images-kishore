name: Check zstd Version

on: [push]

jobs:
  check-zstd-version:
    runs-on: windows-2019

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Check zstd Version
        run: zstd -V
